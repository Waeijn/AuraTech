services:
  # Frontend Service
  frontend:
    image: node:20-alpine
    container_name: auratech-frontend
    working_dir: /app
    volumes:
      - ./auratech-frontend:/app
      - frontend_node_modules:/app/node_modules
    ports:
      - "4000:4000"
    environment:
      - FAST_REFRESH=true
      - PORT=4000
    command: sh -c "npm install && npm start"
    stdin_open: true
    tty: true
    restart: "no"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4000"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Backend Service
  backend:
    build:
      context: ./auratech-backend
      dockerfile: Dockerfile
    container_name: auratech-api-server
    ports:
      - "18082:80"
    volumes:
      - ./auratech-backend/src:/var/www/html
      - backend_vendor:/var/www/html/vendor
    depends_on:
      - db

  # Database Service (MySQL)
  db:
    image: mysql:8.0
    container_name: auratech-mysql-db
    restart: "no"
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: auratech_db
      MYSQL_USER: auratech_user
      MYSQL_PASSWORD: auratech_pass
    ports:
      - "3307:3306"
    volumes:
      - auratech-backend_auratech_db_data:/var/lib/mysql

  # phpMyAdmin
  pma:
    image: phpmyadmin/phpmyadmin
    container_name: auratech-phpmyadmin
    restart: "no"
    ports:
      - "18081:80"
    environment:
      PMA_HOST: db
      MYSQL_ROOT_PASSWORD: rootpassword
    depends_on:
      - db

volumes:
  auratech-backend_auratech_db_data:
    external: true
  frontend_node_modules:
  backend_vendor:
